# 界面验收记录

日期：2026-07-29

## 本次范围

- 删除桌面宠物入口与运行时界面。
- 将图片卡片主题库替换为“浅色主题 / 深色主题”两级选择器。
- 将 Codex CLI 与 ACP 会话的模型选择器移入消息输入框工具栏。
- 将已知 Codex 上游警告本地化，并兼容历史无错误码消息。

## 对照结果

### 主题选择

- 对照附件中的旧图片卡片主题库检查，设置页已不再加载或显示旧主题图片。
- 下拉菜单为两级级联结构：第一列是浅色、深色分组，第二列是各组的 10 个主题。
- 已检查浅色“白瓷”和深色“墨夜”的实际渲染；背景、面板、文本、边框和强调色均同步切换。
- 主题详情只显示名称、分组、说明和三个配色色块，没有遗留的自定义 CSS、上传封面或广告主题入口。

### 模型选择

- 对照附件中的位置标注检查，模型选择器已从会话标题栏移入消息输入框右下工具区。
- 模型选择器位于权限选择器之前，和发送按钮处于同一操作层级。
- 新会话引导页与既有会话共用相同的位置规则；移动端继续使用原有操作面板，避免挤压输入区。

### 汉化与信任提示

- 新产生的三类 Codex 警告通过稳定错误码翻译。
- 历史数据库里 `code=null` 的同类英文警告会按内容前缀识别并显示本地化文本。
- 自动创建的 Codex 临时会话不再把技能写入工作区 `.codex`，避免反复触发项目未信任警告。

## 验证

- TypeScript 类型检查通过。
- UI 定向单元测试通过。
- Electron 生产构建通过。
- TjuaeCore 相关 crate 编译与定向测试通过。
- 已启动桌面开发版完成实际界面检查，并保留程序供人工查看。

---

# 引擎体系迁移验收

日期：2026-08-01

## 本次范围

- 将设置中的“Agent / 智能体连接”统一为“引擎”，并按“内置 / 本地 CLI / 远程服务”组织。
- 将 Native、ACP、A2A 降为引擎的协议标签，不再作为并列产品概念。
- 对话、团队和定时任务只选择助手；所有引擎必须先由助手包装后才能使用。
- 引擎页启动时自动检测，并保留“测试全部”和单引擎重新测试入口。
- 模型目录随检测结果缓存，状态覆盖可用、未安装、需登录、配置异常、协议异常、超时、不可用和未检测。
- 管理 API、桌面桥接和助手公开契约迁移到 engine 命名，不保留旧 `/api/agents/*` 管理路由兼容层。

## 视觉对照

- 参考图：`E:\liangboqiang\Codex\Home\generated_images\019fab0a-c805-7673-94bc-e2bff0f55fea\call_GQaNr3OyOKml1wc2HsSDmU4u.png`
- 实现截图：`E:\liangboqiang\github\tjuae\TjuaeUI\.artifacts\engine-design-qa\implementation-final.jpg`
- 并排对照：`E:\liangboqiang\github\tjuae\TjuaeUI\.artifacts\engine-design-qa\reference-vs-implementation-final.png`
- 使用与参考图一致的 1487 × 1058 视口检查。
- 页面保留既有 TjuaeUI 导航、字号、间距和 1024px 内容宽度；信息架构、操作顺序、分类容器、状态摘要和底部助手约束与选定方案一致。
- 后台目录继续自动扫描本地 CLI；引擎管理页只展示已安装的自动发现项、用户创建项与远程服务，未安装候选不再占用界面。
- 远程服务为空时仍显示分类和添加入口，A2A 不再因无数据而从界面概念中消失。

## 功能与质量验证

- TypeScript 类型检查通过。
- 13 种语言资源一致性检查通过。
- 引擎设置及诊断相关 UI 定向测试 23 项通过。
- Rust API 类型与助手服务测试通过。
- 助手端到端测试 52 项通过。
- 引擎路由端到端测试 14 项通过，并验证旧 `/api/agents/management` 返回 404。
- Electron 生产构建通过。
- 本地 WebUI 使用当前 TjuaeCore 调试二进制完成真实登录、自动检测和页面交互验收。

result: passed

---

# 引擎术语与助手卡片细节验收

日期：2026-08-01

## 本次范围

- 助手编辑页统一使用“引擎”表述，并清理同一流程中的“Agent / 智能体 / 代理名称”残留。
- “添加引擎”菜单使用独立动作文案：“+ A2A 远程服务”和“+ 本地 CLI”，不影响编辑弹窗标题。
- “我的助手”中的 `generated` 引擎助手与 `user` 用户助手卡片均默认显示“去对话”，不再依赖鼠标悬停。
- 助手卡片运行来源标签由“智能体：”统一为“引擎：”。

## 视觉对照

- 引擎编辑区：`C:\Users\ADMINI~1\AppData\Local\Temp\tjuae-engine-detail-qa\comparison-engine-copy.png`
- 添加引擎菜单：`C:\Users\ADMINI~1\AppData\Local\Temp\tjuae-engine-detail-qa\comparison-add-menu.png`
- 助手卡片按钮：`C:\Users\ADMINI~1\AppData\Local\Temp\tjuae-engine-detail-qa\comparison-chat-button.png`
- 实际页面截图：`assistant-engine-editor-final.png`、`engine-add-menu-final.png`、`assistant-cards-final.png`，位于同一临时验收目录。

## 功能与质量验证

- 5 个定向测试文件、44 项测试通过。
- TypeScript 类型检查通过。
- 13 种语言资源一致性检查通过。
- Electron 生产构建通过。
- 本地 WebUI 完成编辑页、菜单与助手卡片交互检查；页面控制台无错误。

final result: passed

---

# 设置界面六项细节收口验收

日期：2026-08-01

## 本次范围

- 远程服务空状态入口统一为“+ A2A 远程服务”，并改为与引擎页一致的轻量操作样式。
- A2A 远程服务编辑器改为标准设置弹窗，压缩信息密度并重组表单、安全选项、发现测试与底部操作区。
- “引擎”选择并入助手“默认配置”，删除独立分类；A2A 助手保留引擎归属但隐藏不适用的本地配置项。
- 主题选择器改为紧凑双栏卡片，补充当前主题说明和色板预览，避免横跨整个内容区。
- “Agent 空闲超时”统一为“本地引擎空闲超时”，明确仅影响本地 CLI 进程，不影响远程 A2A 服务。
- 浏览器通知保留真实功能，但合并为一个状态明确的设置开关；针对未授权、已拒绝、不支持与非安全上下文分别给出可执行反馈。

## 视觉对照

- 逐项对照用户提供的六张参考截图与最终浏览器页面，检查术语、层级、控件风格、留白、信息密度和按钮可见性。
- A2A 编辑器在 1280 × 720 视口下完整显示核心字段、折叠安全项、发现测试区和底部操作按钮。
- 助手编辑页只保留一张“默认配置”卡片，引擎、模型、权限、技能和 MCP 形成同一配置层级。
- 主题设置、空状态入口和通知设置均沿用现有圆角、边框、字号、间距和状态色，不引入独立视觉体系。

## 功能与质量验证

- 5 个定向测试文件、42 项测试通过。
- TypeScript 类型检查通过。
- 13 种语言资源一致性与类型同步检查通过。
- Electron 生产构建通过。
- 本地 WebUI 完成引擎页、A2A 编辑器、助手默认配置、外观设置和系统通知交互检查；页面控制台无错误。

final result: passed
