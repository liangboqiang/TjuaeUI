# 文件预览

TjuaeUI 的文件预览由本地渲染能力完成，不把用户文档上传到外部 Office
预览服务。文本、代码、图片、HTML、PDF 与常见办公文档都通过桌面端内部组件
或 TjuaeCore 的本地转换接口处理。

## 边界

- 渲染进程只负责展示，不直接访问 Node.js 文件系统。
- 文件读取、快照和办公文档转换通过带类型约束的 IPC 与 TjuaeCore 完成。
- 预览流程不得依赖外部 CDN、在线文档转换服务或旧产品接口。
- 新增格式时必须补充单元测试，并验证离线环境下的失败提示。
